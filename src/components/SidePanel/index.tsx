import * as React from'react'

interface ISidePanelProps {

}

export default (props: ISidePanelProps) => {

    return (
        (
            <div className="sidePanelWrapper">
           
            </div>
        )
    )
}